/*
Ejercicio 3
*/

// Crea dos variables, una llamada numero1 y otra llamada numero2.
const numero1 = 1;
const numero2 = 2;

// Suma las dos variables y muestra el resultado por consola.
const suma = numero1 + numero2;

// Crea otras variables de tipo number y realiza las operaciones de resta, multiplicación y división. Mostrando el resultado por consola.
const resta = numero1 - numero2;
const multiplicacion = numero1 * numero2;
const division = numero1 / numero2;

console.log(`La suma de ${numero1} + ${numero2} es ${suma}`);
console.log(`La resta de ${numero1} - ${numero2} es ${resta}`);
console.log(`La maultiplicación de ${numero1} * ${numero2} es ${multiplicacion}`);
console.log(`La división de ${numero1} / ${numero2} es ${division}`);